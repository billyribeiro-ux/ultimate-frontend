<svelte:head><title>Module 9A · Data Loading (load)</title></svelte:head>
<section class="page stack">
	<p class="eyebrow">Module 9A</p>
	<h1>Data Loading (traditional <code>load()</code>)</h1>
	<p><code>+page.ts</code> vs <code>+page.server.ts</code>, auto-generated <code>$types</code>, parallel loading, <code>depends()</code> + <code>invalidate()</code>, streaming promises, <code>prerender = true</code>.</p>
	<p class="stub-notice">Lesson routes scaffolded — content produced in a later pass.</p>
	<p><a href="/modules">← All modules</a></p>
</section>
<style>
	section { --color-brand: oklch(70% 0.18 250); }
	.eyebrow { font-size: var(--text-sm); color: var(--color-brand); text-transform: uppercase; letter-spacing: 0.08em; }
	.stub-notice { color: var(--color-text-muted); font-style: italic; }
</style>
